# react-skeleton
```
mkdir react-skeleton
git init
git remote add origin https://github.com/justinhsz/react-skeleton.git
git add README.md
git status
git commit -m "initial commit"
git push origin master
```
